image: node:20

stages:
  - build

cache:
  key:
    files:
      - package-lock.json
  paths:
    - node_modules/

build_app:
  stage: build
  script:
    - npm ci
    - npm run build
  artifacts:
    when: always
    expire_in: 1 week
    paths:
      - build/
      - .svelte-kit/output/
